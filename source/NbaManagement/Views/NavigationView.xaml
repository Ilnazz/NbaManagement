<UserControl x:Class="NbaManagement.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Margin="0,0,0,5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Image Source="{StaticResource Images.Logo}" Margin="0,0,5,0"/>
                <TextBlock Text="NBA Management System"/>
            </StackPanel>
                
            <TextBlock HorizontalAlignment="Center" Text="{Binding Title}" Style="{StaticResource Styles.HeaderTextBlock}"/>
                
            <Button HorizontalAlignment="Right" Content="Back" Command="{Binding NavigateBackCommand}"/>
        </Grid>
        
        <ContentPresenter Grid.Row="1" Content="{Binding CurrentViewModel}"/>
    </Grid>
</UserControl>